TELEGRAM_TOKEN=your_telegram_token_from_botfather
WEATHER_API_KEY=your_api_key_from_openweathermap

# PostgreSQL (для sql_basics.py / sql_groupby.py / sql_join.py)
DB_NAME=weather_db
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432

# Crypto (урок 5: crypto_basics.py)
# USER_ID_SALT — сіль для хешування user_id у bot_log.csv (приватність).
# Згенерувати: python -c "import secrets; print(secrets.token_hex(32))"
USER_ID_SALT=
